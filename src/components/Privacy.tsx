import type { ChangeEvent } from 'react'

type Props = { onBack: () => void; onReset: () => void; onExport: () => void; onImport: (event: ChangeEvent<HTMLInputElement>) => void }

export function Privacy({ onBack, onReset, onExport, onImport }: Props) {
  return <section className="privacy-page page-narrow"><button className="back-button" onClick={onBack}>← Back</button><p className="kicker">Privacy and safety</p><h2>A practice should feel private.</h2><p className="privacy-lede">Lived-situation history is stored in this browser without an account. Your draft is sent to the hosted model only when you ask for a model review. Nothing is published automatically. This is not a therapist, crisis service, diagnosis, or replacement for clinical care.</p><div className="privacy-list"><div><span>01</span><div><h3>Local by default</h3><p>Your reflection draft, model, and history are stored in browser storage on this device. Hosted deployments may still have infrastructure-level access to requests.</p></div></div><div><span>02</span><div><h3>You stay in control</h3><p>The AI proposes interpretations and constructed probes. You can accept, revise, reject, skip, or leave them unresolved.</p></div></div><div><span>03</span><div><h3>Support stays human-led</h3><p>Use this practice to prepare observations and questions for a qualified clinician. It does not assess risk, provide treatment, or detect crises.</p></div></div></div><div className="privacy-actions"><button className="secondary-button" onClick={onExport}>Export my practice</button><label className="secondary-button file-button">Import practice<input type="file" accept="application/json,.json" onChange={onImport} /></label><button className="danger-button" onClick={onReset}>Delete local practice</button></div></section>
}
